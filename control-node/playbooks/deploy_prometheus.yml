---
- name: Deploy Prometheus configuration and rules
  hosts: monitor_nodes
  gather_facts: false
  become: true

  vars_prompt:
    - name: ansible_become_password
      prompt: "BECOME password"
      private: true

  roles:
    - prometheus
